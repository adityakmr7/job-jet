/** Shared by the server config and the forms. NIST SP 800-63B recommends
 *  length over composition rules; 10 is comfortably above the 8 minimum. */
export const MIN_PASSWORD_LENGTH = 10;
export const MAX_PASSWORD_LENGTH = 128;
